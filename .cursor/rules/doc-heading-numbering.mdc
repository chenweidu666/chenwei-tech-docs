---
description: 技术文档 Markdown 标题层级编号（1. / 1.1. / 1.1.1.）
globs: docs/**/*.md
alwaysApply: false
---

# 技术文档标题编号规范（3_技术文档）

编辑 `docs/` 下 Markdown 时，**每篇文档内部**采用统一层级编号（**与文件名中的篇号如 5.2 脱钩**），格式为 **阿拉伯数字 + 英文句点 + 空格 + 标题**，层级与 Markdown 标题级 `#` 数一致。

## 层级格式

| Markdown | 编号格式 | 示例 |
|----------|----------|------|
| H1 `#` | `{1}.` | `# 1. MCP 协议` |
| H2 `##` | `{1}.{2}.` | `## 1.1. MCP 是什么` |
| H3 `###` | `{1}.{2}.{3}.` | `### 1.2.1. Tools（工具调用）` |
| H4 `####` | `{1}.{2}.{3}.{4}.` | `#### 1.2.1.1. 子款标题` |
| H5–H6 | 继续向下缀一层序号 | 类推 |

## 计数规则

- **每篇仅一个 H1**：正文 + 附录全篇只允许 **第一个** `# 标题`；附录里若粘贴了 `# 某某指南`，须先改为 `##`（或由 `scripts/renumber_headings.py` 自动降为 `##` 后再编号），避免出现与篇首平级的第二个 `# 2.`。
- **每篇文档独立计数**：该唯一 H1 固定为 **`# 1. …`**。
- **子级归零**：出现新的 H2 时，其下 H3、H4 计数重新从 1 递增；出现新 H3 时，其下 H4 从 1 递增。
- **缺级补齐**：若文首直接从 `##` 开始（无 H1），脚本/人工视为隐含 **`# 1.`**，子级仍写作 `1.1.`、`1.1.1.`。
- **句点**：编号末级后必须有 **句点**，再接空格与标题正文，例如 `## 1.1. 标题`，**不要**写成 `## 1.1 标题`。

## 附录与长文粘贴区

- 附录入口仍用 **`##`**，按顺序占用下一个 **1.n.**（如 `## 1.8. 附录：…`）。
- 附录内后续标题按 **实际 `#` 层级**继续编号（可能出现连续的 `## 1.9.`、`## 1.10.`，与正文同级；若希望缩进为附录子节，需把内容改为 `###` 层级）。

## 代码块

- 围栏代码块（` ``` `）内的 `#` / `##` **一律不参与**编号（注释、提示词等保持原样）。

## 文首元信息

- 「上一节 / 下一节 / 导读」等放在表格或正文，**不要用标题级 `#`**，避免打乱层级计数。

## 批量整理

- 使用仓库内 `scripts/renumber_headings.py`：在 `docs/` 下遍历 `.md`，按标题深度重算编号，并剥除标题上旧的 `5.2.1.`、`一、` 等前缀后再写入新编号。

## `docs/README.md`

- 站点总览可用单行 H1：**`# 1. CW · Tech Docs`**（全库仅一篇「第 1 章」式总览，不与各文章内 `1.` 冲突——因各文章为独立文件）。
