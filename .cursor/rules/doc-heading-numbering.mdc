---
description: 技术文档 Markdown 标题分级编号（篇号 + 节号）
globs: docs/**/*.md
alwaysApply: false
---

# 技术文档标题编号规范（3_技术文档）

编辑 `docs/` 下 Markdown 时，**正文标题**采用「篇号 + 分级序号 + 句点 + 空格」形式，与 GB/T 式技术书写习惯一致，便于目录、交叉引用与 diff 对照。

## 1. 篇号（取自文件名）

- 文件名形如 `1.1_大模型基础与架构.md`、`5.2_MCP协议.md`，篇号为 **`1.1`、`5.2`**（不含下划线后标题）。
- **一级标题（H1）**：全篇唯一，格式为 `# {篇号}. {篇名}`。  
  - 正确：`# 1.1. 大模型基础与架构`  
  - 错误：`# 1.1 大模型基础与架构`（缺篇号后句点）、`# 第一章 …`（不用中文章序替代篇号）。

## 2. 正文章节（H2 / H3 / H4）

- **H2**：`## {篇号}.{节号}. {标题}`，节号为从 1 递增的整数。原稿「一、二、三…」对应节号 1、2、3…。  
  - 示例：`## 5.1.3. 工具调用设计`（对应原「三、工具调用设计」）。
- **H3**：`### {篇号}.{节号}.{小节号}. {标题}`。原稿 `### 2.1` 表示第 2 节下第 1 小节，应写为 `### 5.1.2.1. ReAct（推理 + 行动）`。  
  - **小节号**与篇号、节号之间均用 **句点** 分隔；**标题前保留一个空格**。
- **H4**：`#### {篇号}.{节号}.{小节号}.{款号}. {标题}`，在同一 H3 下款号从 1 递增。原稿无数字时可仅加款号序。

## 3. 附录与「📎 参考资料」长文

- 遇到 **`## 📎 参考资料：…`** 或 **`## 附录：…`**，视为**附录入口**，仍用篇号续接节号：例如 `## 5.2.8. 附录：MCP 技术介绍与实践（扩展阅读）`。
- **附录入口之后**粘贴的长文、面试稿等：为控制改动面与锚点稳定性，**可保留其内部原有 `## / ###` 结构**；新建内容时建议改为在篇号树下续写（`### {篇号}.{节}.{小}. …`），避免与正文节号冲突。
- 附录内若单独出现 **`# 某某指南`** 级标题，应降为 `##` 或 `###` 并纳入篇号树，**禁止**与篇首 H1 平级并列。

## 4. 代码块与非标题行

- **围栏代码块**（` ``` `）内的 `#`、`##` 一律**不参与**标题编号（多为 shell 注释或提示词）。
- 普通行以 `#` 开头但不是 Markdown 标题语法者（罕见）不要改动。

## 5. 导航与文首元信息

- 文首「上一节 / 下一节 / 导读」等：优先用 **表格**（见 `5.2_MCP协议.md` 文首），不用多级引用块堆叠。

## 6. 首页 `docs/README.md`

- 可使用 **`# 1. CW · Tech Docs`**（将全库视为总览篇），或保持无篇号短标题；**勿**与各篇 H1 篇号重复冲突。

---

**执行约定**：批量整理时可用仓库内 `scripts/renumber_headings.py`（若存在）对正文做转换；人工复核附录与超大文件中的边界情况。
