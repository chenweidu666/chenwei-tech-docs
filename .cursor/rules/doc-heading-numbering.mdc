---
description: 技术文档篇名与章节编号（对齐 1.1 大模型基础与架构）
globs: docs/**/*.md
alwaysApply: false
---

# 技术文档格式规范（3_技术文档）

Canonical 示例：`docs/01_基础层/1.1_大模型基础与架构.md`（现为 **导读**）；同主题分篇为 `1.1_01`～`1.1_03`、`1.1_05`（**KV Cache 与推理加速** 已迁至 `docs/03_部署与推理/3.1_KVCache与推理加速.md`），每篇自有篇名 div，正文大节仍从 `# 1.` 起编。

## 篇名（不要用 Markdown 一级标题当篇名）

- 文首一行（可前有空白行）：  
  `<div style="text-align: center; font-size: 2rem; font-weight: 700; margin-bottom: 0.5rem;"><strong>篇名文字</strong></div>`
- **不要**使用 `# 篇名`；篇名**不带**章节序号（如不要写「1. 大模型基础与架构」作篇名，除非产品上就要求篇名含序号）。
- 篇名与正文第一个大节 `# 1. …` 之间**空一行**。

## 文首 / 文末不要出现的内容

- 文首 blockquote：`> **导航**：`、`> **本章导读**：`、`> **关键词**：`、`> **本章性质**：` 等（冒号可为中文 `：`）。
- 文末 blockquote：`> **下一章**：`、`> **下一节**：`、`> **上一章**：`、`> **上一节**：`。
- 文首「上一节 / 下一节 / 导读」类**导航 Markdown 表**（如旧版 5.2 顶部）。

## 正文章节（大节 / 小节）

- **大节**：按在文内出现顺序，使用 Markdown ATX **`# 1. 标题`**、`# 2. 标题`、…（全文从 1 递增）。  
  - 迁移前若是 `<h1 align="center">标题</h1>`，应改为上述 `# N.` 形式，**不要**再保留居中 HTML 一级标题。
- **小节**：隶属「最近一个大节 `# N.`」下的 **`## N.1. 标题`**、`## N.2.`、…（`N` 与当前大节一致，子序号递增）。
- **`###` 及以下**：保持比 `##` 更深一级即可；**不要**把代码围栏里的 `#` 当标题改写。

## 批量应用

- 执行：`python3 scripts/apply_doc_format.py`（自 `docs/` 递归处理 `.md`）。
- **`scripts/normalize_headings.py`**、**`scripts/renumber_headings.py`** 为旧版（居中 `<h1>`、去数字等）逻辑；**与当前规范冲突时不要执行**，除非有意回滚样式。

## 渲染说明

- 篇名使用内联 `style` 的 `<div>`，Docsify / 多数静态站可渲染；若主题过滤 `style`，再在 `index.html` 中为篇名单独加 class 样式。
